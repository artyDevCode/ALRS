@model ALRSSystem.Models.RequestorPosition

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>RequestorPosition</h4>
	<hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.RequestorPositionName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.RequestorPositionName)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.ID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
